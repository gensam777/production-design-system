# Themes

This folder will hold theme/mode overrides (e.g. `light.json`, `dark.json`) that remap
semantic tokens for a given theme or brand.

Deliberately empty for now — dark mode / multi-theme implementation is deferred until
after the base token pipeline and Color foundation are in place.
